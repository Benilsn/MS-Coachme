
spring:
  application:
    name: email_sender

  config:
    import: optional:configserver:http://localhost:8888

  datasource:
#    url: jdbc:postgresql://172.21.0.20:5432/ms_emails
    url: jdbc:postgresql://localhost:5432/ms_emails
    username: 'coachme'
    password: 'root'

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: 'true'
    hibernate:
      ddl-auto: update
    show-sql: 'true'




